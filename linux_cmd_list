linux命令
1、iptables禁止某个ip访问
2、tcpdum&wireshark网络抓包
3、strace定位诊断系统调用
4、dd生成指定大小的文件，用以测试
5、linux系统日志/var/log/message
6、查看与删除共享内存
7、绑定虚拟网卡、浮动ip
8、查看系统shell cat /etc/shells
9、双机信任关系建立，ssh对端不用输yes和密码


linux系统编程
1、对ps隐藏启动参数（c语言）覆盖argv[i]以特殊字符
2、通过文件锁实现单实例
3、多线程编程pthread.h
4、共享内存
5、socket编程
6、c获取环境变量
7、获取文件用户
8、gdb调试
9、利用单维数组，构造已知大小的多维数组，利用宏来实现变量地址
10、c在当前目录中查找文件，过滤.和..目录，递归查找，利用函数指针（指定递归目录具体干什么活），递归变量目录用例外函数实现。
11、linux系统信号
12、linux的c语言，函数指针free掉后依然指向原地址，所以未置null可能造成野指针调用。
13、自旋锁
14、内存LRU算法
15、调试器是怎么工作的，gdb
16、宏打印可变参数的日志，文件名，文件行，时间等封装__date__ __FILE__ __LINE__
17、获取系统时间，两种，一种获取从开机到目前的相对时间，用户改变系统时间不影响时间处理判断，二获取系统时间，可能会被系统时间改动影响
18、memcpy，越界，地址重复
19、sprintf，越界后最后一位不能保证有\0
20、printf打印对齐，%-8u


脚本
1、awk编程，分割
2、sed行行处理
3、绑定浮动ip
4、正则表达式匹配
5、cut剪接字符串，
6、shell ${}进行截断，通过##/%%完成
7、长字符串用EOF来包括

工具使用
1、gvim
2、atom和ctags来完成脚本调用查看
    ctags.exe -R --language-force=sh *
3、source insight、pclint
4、wins cgwin
5、secureCRT
6、Notepad++
7、比较Mybase desktop，破解，nyfedit.ini中的App.UserLic.FirstUseOn，将这个时间戳随新的改变

其他
1、字符编码。ascii码、unicode、utf-8
2、MySQL开发实践8问，你能hold住几个？
3、高性能服务器架构思路（三）-分布式系统概念
4、万亿级调用下的优雅-微信序列号生成器架构设计及演变

技术网站
1、stackoverflow.com   google  site://stackoverflow.com 搜索特定网站内容
2、Leet Code 网络oj
3、quora-英文版知乎
4、cplusplus.com-c++所有api网站
